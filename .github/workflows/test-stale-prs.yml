name: Stale PRs
on:
  workflow_dispatch:
  
jobs:
  handle_stale_prs:
    name: handle stale PRs
    uses: matt-lewis-testing-org/reusable-actions/.github/workflows/utility-stale-prs.yml@main
    secrets: inherit
    permissions:
      issues: write
      pull-requests: write
    with:
      warn_max_days: 30
      close_max_days: 60
