name: CI Example

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  wait:
    runs-on: ubuntu-latest
    steps:
    - run: sleep 30s
    - name: output test
      uses: matt-lewis-testing-org/reusable-actions/.github/actions/test-action@67b5f39d01be0ed720d6839e5bfd302f8d811b07
